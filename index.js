// const table = document.querySelector('.table');
// const tableLegs = document.querySelector('.table-legs');
// const raiseButton = document.getElementById('raise-table');
// const lowerButton = document.getElementById('lower-table');

// let tablePosition = 0; // Начальная позиция стола

// raiseButton.addEventListener('click', () => {
//   tablePosition -= 10; // Поднимаем стол на 10px (или другое значение)
//   table.style.transform = `translateY(${tablePosition}px)`; // Применяем смещение
// });

// lowerButton.addEventListener('click', () => {
//   tablePosition += 10; // Опускаем стол на 10px
//   table.style.transform = `translateY(${tablePosition}px)`; // Применяем смещение
// });
